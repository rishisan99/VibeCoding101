# Workflow Agent

Simple Streamlit app for a small `planner -> executor` workflow agent using OpenAI.

## Setup

```bash
uv sync
```

## Run

```bash
uv run streamlit run app.py
```

## Test Sample

Use the `Load Sample Workflow` button in the app.
