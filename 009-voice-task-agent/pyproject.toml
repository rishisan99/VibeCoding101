[project]
name = "009-voice-task-agent"
version = "0.1.0"
description = "Simple voice notes to tasks app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.32.0",
    "streamlit>=1.56.0",
]
